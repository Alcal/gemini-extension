FROM postgres:16-alpine

# Seed the catalog on first init. init.sql is generated by
# scripts/extract_from_excel.py from the product Excel workbook.
COPY init.sql /docker-entrypoint-initdb.d/10-init.sql
